import Box from "@mui/material/Box";

// component
import CustomCard from "components/GlobalComponents/Card/CustomCard";

function CalegSuara({ caleg }) {
  return (
    <div>
      <CustomCard title={`Perolehan Suara`} showDivider={true}>
        <h3>DIAGRAM SUARA BELUM TERSEDIA</h3>
      </CustomCard>
    </div>
  );
}

export default CalegSuara;
